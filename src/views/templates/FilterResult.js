/**
 * CREATE FILTER RESULT
 * @returns div
 */
const FilterResult=()=>{
    const container = document.createElement('div');
    container.id='filterResult';
    container.className='container-filterBar__result';

    return container
}

export default FilterResult